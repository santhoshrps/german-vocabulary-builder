{
  "name": "german-vocabulary-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test:publication": "node --test test/publication.test.mjs",
    "test:snapshots": "node --test test/snapshot-publication.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.104.0"
  }
}
